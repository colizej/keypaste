import Foundation

// Sprint 1 will populate this directory with:
//   - EventTap.swift        (CGEventTap setup, accessibility check)
//   - StatusBarController.swift (NSStatusBar item, menu)
//   - PasteStrategy.swift   (IME-safe insertion via temporary pasteboard)
//   - PermissionsChecker.swift
//
// This placeholder file exists so SwiftPM does not complain about the
// empty target directory.

enum SystemPlaceholder {
    static let note = "System module — implementations land in Sprint 1."
}
